# Cloudflare Pages config.
# The KV namespace id below was created with `wrangler kv namespace create PHARMABRO_KV`.
# Bindings in wrangler.toml take precedence over the dashboard, so when this
# file exists you must declare them here.

name = "pharmabro"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"

[[kv_namespaces]]
binding = "PHARMABRO_KV"
id = "a09d2b2103724d6995c4a221daca8b02"
